import React, { useEffect } from "react";
import withAuth from "hoc/withAuth";
const ListPartyHaan = () => {
  useEffect(() => {}, []);
  return <div>list</div>;
};

export default withAuth(ListPartyHaan);
